services:
  opencode-remote:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: opencode-remote
    ports:
      - "1657:1657"
    restart: unless-stopped
    environment:
      HOST: 0.0.0.0
      PORT: 1657
